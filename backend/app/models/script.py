# MongoDB script document helpers live in app/models/user.py
# (consolidated into a single models file — kept for import compatibility)
from app.models.user import make_script, _str_id, now_utc  # noqa: re-export
